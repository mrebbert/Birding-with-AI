# Secrets: keep every real value out of the repository
.env
*.env
!*.env.example
secrets/

# Local working copies produced by apply-placeholders.sh
build/
out/

# Container data at the stack root, never commit.
# Anchored with a leading slash so img/birdnet-go/ and img/saezuri/ stay tracked.
/birdnet-go/
/saezuri/
*.db
*.sqlite
*.bak

# Editor and OS noise
.DS_Store
.idea/
.vscode/
*.swp
